\subsection{Code-generated exhibits}

All tables and figures below are automatically generated by the MATLAB suite and written to
\texttt{out/exhibits/}. To refresh them, run \texttt{RUN\_ALL.m} (one click) or the driver
\texttt{S20\_main\_T\_scaling\_stage7.m} (main exhibits only).

\paragraph{Main scaling exhibit.}
\begin{table}[!ht]
\centering
\caption{Scaling in $T$ with constant batch length $L$. Entries are Monte Carlo means with MCSE in parentheses.
We report Best(static), Policy, Oracle(ctx), the excess risk $\text{Gap}=\text{Policy}-\text{Best(static)}$,
context value $\text{CtxGain}=\text{Best(static)}-\text{Oracle(ctx)}$, oracle regret
$\text{Regret}=\text{Policy}-\text{Oracle(ctx)}$, win rate $\mathbb{P}(\text{Gap}<0)$, and a sparsity diagnostic
for context$\times$arm counts. Since $\text{Gap}=\text{Regret}-\text{CtxGain}$, the win rate equals
$\mathbb{P}(\text{Regret}<\text{CtxGain})$.}
\vspace{0.25em}
\IfFileExists{\ExhibitDir tab_paper_scaling.tex}{
  \input{\ExhibitDir tab_paper_scaling.tex}
}{
  \fbox{\parbox{0.9\linewidth}{Missing file: \texttt{tab\_paper\_scaling.tex}. Run \texttt{S20\_main\_T\_scaling\_stage7.m}.}}
}
\end{table}

\begin{figure}[!ht]
\centering
\caption{Performance as a function of $T$ under Scenario E (baseline) and Scenario E2 (partial/delayed feedback).
Left panels plot oracle regret $\text{Regret}=\text{Policy}-\text{Oracle(ctx)}$; right panels plot win rate
$\mathbb{P}(\text{Gap}<0)$. Using $\text{Gap}=\text{Regret}-\text{CtxGain}$ with
$\text{CtxGain}=\text{Best(static)}-\text{Oracle(ctx)}$, the win rate equals $\mathbb{P}(\text{Regret}<\text{CtxGain})$.
Error bars are $\pm 2$ MCSE.}
\vspace{0.25em}
\IfFileExists{\ExhibitDir fig_paper_scaling.pdf}{
  \includegraphics[width=0.98\linewidth]{fig_paper_scaling.pdf}
}{
  \fbox{\parbox{0.9\linewidth}{Missing file: \texttt{fig\_paper\_scaling.pdf}. Run \texttt{S20\_main\_T\_scaling\_stage7.m}.}}
}
\end{figure}

\ifShowCalibration
\paragraph{Sensitivity (optional).}
\begin{table}[!ht]
\centering
\caption{Sensitivity around $(\#\text{bins},c)$ at the calibration $T$ (Monte Carlo means with MCSE in parentheses).}
\vspace{0.25em}
\IfFileExists{\ExhibitDir tab_paper_calibration.tex}{
  \input{\ExhibitDir tab_paper_calibration.tex}
}{
  \fbox{\parbox{0.9\linewidth}{Missing file: \texttt{tab\_paper\_calibration.tex}.}}
}
\end{table}
\fi
